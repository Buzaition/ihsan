# إحسان — Holy Quran & Azkar Web App

**إحسان** هو موقع قرآني عربي بسيط وهادئ لقراءة القرآن الكريم، البحث في الآيات، قراءة الأذكار، متابعة مواقيت الصلاة، استخدام المسبحة الإلكترونية، وحفظ المفضلة وموضع القراءة.

تم تطوير التطبيق كعمل نافع وإهداء إلى روح الجدة:

> **إحسان إبراهيم الخبيري — رحمها الله وغفر لها وأسكنها فسيح جناته.**

---

## فكرة المشروع

المشروع عبارة عن تطبيق ويب Frontend فقط يعمل بملفات:

- `index.html`
- `styles.css`
- `app.js`
- `manifest.webmanifest`
- مجلد `assets`

ولا يحتاج إلى Backend أو قاعدة بيانات خارجية. يتم حفظ بيانات المستخدم مثل آخر موضع قراءة، المفضلة، الإعدادات، والمسبحة باستخدام `localStorage` داخل المتصفح.

---

## المميزات

### القرآن الكريم

- فهرس كامل لسور القرآن الكريم.
- قراءة السور آية آية في بطاقات منفصلة.
- وضع قراءة متصلة للسورة كاملة.
- عرض البسملة بشكل صحيح:
  - تظهر مستقلة في منتصف القراءة في كل السور.
  - لا تظهر مستقلة في سورة الفاتحة لأن الآية الأولى هي البسملة.
  - لا تظهر في سورة التوبة.
- أرقام آيات مزخرفة في وضع القراءة المتصلة.
- السورة السابقة والسورة التالية.
- حفظ موضع القراءة تلقائيًا.
- شاشة كاملة للقراءة.
- عرض التفسير أسفل الآية في القراءة المتصلة.
- تشغيل صوت الآية.
- منع تداخل الأصوات عند تشغيل أكثر من آية.
- القارئ الافتراضي: **الحصري**.

### البحث

- بحث في القرآن الكريم.
- البحث غير حساس للتشكيل والهمزات والمدود وعلامات الوقف.
- عرض النتائج بشكل واضح مع اسم السورة ورقم الآية.

### الأذكار

- قسم كامل للأذكار.
- عرض أبواب الأذكار في بطاقات منظمة.
- عند اختيار باب، تظهر الأذكار أسفل نفس مكان الاختيار.
- عداد تكرار لكل ذكر.
- نسخ ومشاركة وحفظ الأذكار في المفضلة.

### المسبحة الإلكترونية

- عداد تسبيح كبير وسهل الاستخدام.
- اختيار ذكر سريع.
- تصفير العداد.
- حفظ الذكر في المفضلة.

### مواقيت الصلاة

- عرض الساعة الحالية.
- عرض تاريخ اليوم.
- عرض التاريخ الهجري.
- عرض مواقيت الصلاة.
- عرض الصلاة القادمة.
- عرض الوقت المتبقي على الصلاة القادمة.
- دعم استخدام الموقع الجغرافي عند موافقة المستخدم.

### المفضلة والمتابعة

- حفظ الآيات والأذكار.
- فتح موضع الآية من المفضلة.
- حذف عنصر من المفضلة أو حذف الكل.
- متابعة ختمة بسيطة.
- حفظ آخر موضع قراءة تلقائيًا.

### التصميم وتجربة الاستخدام

- واجهة عربية RTL.
- تصميم هادئ مناسب لمحتوى القرآن والأذكار.
- وضع ليلي / نهاري.
- متجاوب مع الهاتف، التابلت، والكمبيوتر.
- أيقونة مخصصة للتطبيق.
- مناسب للنشر على GitHub و Vercel.

---

## هيكل الملفات

```text
project-root/
├── index.html
├── styles.css
├── app.js
├── manifest.webmanifest
├── README.md
└── assets/
    ├── favicon-32.png
    ├── icon-192.png
    ├── icon-512.png
    └── ihsan-icon.png
```

---

## التشغيل محليًا

يفضل تشغيل المشروع من خلال سيرفر محلي، وليس بفتح ملف `index.html` مباشرة، لأن بعض المتصفحات تمنع طلبات البيانات عند التشغيل من `file://`.

### الطريقة الأولى باستخدام Python

افتح Terminal داخل مجلد المشروع، ثم شغّل:

```bash
python -m http.server 5500
```

ثم افتح في المتصفح:

```text
http://localhost:5500
```

### الطريقة الثانية باستخدام VS Code

يمكنك استخدام إضافة **Live Server** ثم تشغيل المشروع من ملف `index.html`.

---

## النشر على GitHub

1. أنشئ Repository جديد على GitHub.
2. ارفع ملفات المشروع في جذر المستودع:
   - `index.html`
   - `styles.css`
   - `app.js`
   - `manifest.webmanifest`
   - `assets/`
   - `README.md`
3. تأكد أن ملف `index.html` موجود في جذر المشروع أو اضبط إعدادات النشر حسب مكانه.

مثال أوامر Git:

```bash
git init
git add .
git commit -m "Initial Ihsan Quran web app"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY.git
git push -u origin main
```

---

## النشر على Vercel

المشروع Static Frontend، لذلك لا يحتاج Build Command.

1. ادخل على Vercel.
2. اختر **New Project**.
3. اختر Repository الخاص بالمشروع من GitHub.
4. إذا كانت الملفات في جذر المستودع، اترك Root Directory كما هو.
5. إذا كانت الملفات داخل مجلد مثل `ihsan`، اجعل Root Directory هو اسم هذا المجلد.
6. Framework Preset: اختر **Other** أو اتركه كما يحدده Vercel.
7. Build Command: اتركه فارغًا.
8. Output Directory: اتركه فارغًا أو اجعله `.` إذا طُلب منك.
9. اضغط Deploy.

بعد النشر، سيعمل الموقع عبر HTTPS، وهذا مهم لميزات مثل تحديد الموقع الجغرافي لمواقيت الصلاة.

---

## ملف vercel.json اختياري

يمكن إضافة ملف `vercel.json` في جذر المشروع إذا أردت إعدادات بسيطة للنشر:

```json
{
  "cleanUrls": true,
  "trailingSlash": false,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        }
      ]
    }
  ]
}
```

هذا الملف اختياري، والموقع يمكن أن يعمل بدونه.

---

## مصادر البيانات

يعتمد المشروع على مصادر خارجية لجلب بيانات القرآن، الصوت، الأذكار، ومواقيت الصلاة. لذلك يحتاج الموقع إلى اتصال بالإنترنت حتى تعمل كل الخصائص بشكل كامل.

> ملاحظة مهمة: نص القرآن الكريم لا يتم تعديله يدويًا داخل المشروع. يجب دائمًا الاعتماد على مصدر موثوق ومراجعة أي بيانات دينية قبل النشر العام.

---

## الخصوصية

- لا يوجد تسجيل دخول.
- لا يوجد Backend.
- لا يتم إرسال بيانات شخصية إلى قاعدة بيانات خاصة بالمشروع.
- المفضلة والإعدادات وآخر موضع قراءة يتم حفظهم محليًا في المتصفح باستخدام `localStorage`.
- ميزة مواقيت الصلاة قد تطلب إذن الموقع الجغرافي من المستخدم، ويتم استخدامه فقط لحساب مواقيت الصلاة.

---

## طريقة استخدام الموقع

1. من الصفحة الرئيسية اضغط **ابدأ القراءة** للانتقال إلى آخر موضع محفوظ أو فهرس السور.
2. اختر السورة التي تريد قراءتها.
3. استخدم وضع القراءة المتصلة أو قراءة البطاقات حسب تفضيلك.
4. اضغط على الآية في القراءة المتصلة لعرض التفسير أسفلها.
5. استخدم البحث للوصول إلى آية أو كلمة داخل القرآن.
6. افتح قسم الأذكار واختر الباب المناسب.
7. استخدم المسبحة الإلكترونية للتسبيح اليومي.
8. افتح مواقيت الصلاة لمعرفة الصلاة القادمة والوقت المتبقي عليها.
9. استخدم المفضلة لحفظ الآيات أو الأذكار المهمة.

---

## المطور

تم التطوير بواسطة:

**أبوزيد أبوالسعد سعد**

سبب التطوير:

> تم إنشاء هذا التطبيق ليكون عملًا نافعًا وصدقة جارية بإذن الله، يجمع القرآن الكريم، الأذكار، مواقيت الصلاة، والمسبحة الإلكترونية في تجربة بسيطة وهادئة، ويكون باسم **إحسان** إهداءً إلى روح الجدة **إحسان إبراهيم الخبيري** رحمها الله.

---

## ملاحظات قبل النشر الرسمي

- راجع ظهور الآيات والبحث على الهاتف والكمبيوتر.
- جرّب الموقع بعد النشر على Vercel لأن بعض الخصائص تحتاج HTTPS.
- راجع مصادر الأذكار والتفسير شرعيًا قبل الاستخدام العام.
- تأكد أن الأيقونة تظهر بشكل صحيح في المتصفح وعلى الهاتف.
- جرّب أذونات الموقع الجغرافي لمواقيت الصلاة.

---

## الترخيص

هذا المشروع مخصص للاستخدام الخيري والتعليمي. يمكنك تعديل أو تطوير الكود بما لا يخل بحرمة النص القرآني أو صحة الأذكار والمحتوى الديني.

---

## دعاء

اللهم اجعل هذا العمل في ميزان حسنات من ساهم فيه، واجعله صدقة جارية لروح **إحسان إبراهيم الخبيري**، واغفر لها وارحمها وعافها واعف عنها، وأكرم نزلها، ووسع مدخلها، واجمعنا بها في الفردوس الأعلى من الجنة.

## تحديث الواجهة الرئيسية والقائمة الجانبية

- تم إعادة تصميم واجهة الرئيسية لتقليل الفراغ الكبير داخل الـ Hero وجعلها أكثر احترافية على سطح المكتب.
- تم تقليل طول واجهة الرئيسية على الهاتف وتحسين ترتيب العناصر والأزرار.
- تم جعل `app-header` في الرئيسية أكثر انسيابية مع حركة دخول خفيفة.
- تم تحويل قائمة التنقل على الموبايل إلى قائمة جانبية RTL تفتح بزر القائمة بدل أخذ مساحة كبيرة أعلى الشاشة.
- يتم إغلاق القائمة الجانبية تلقائيًا عند اختيار أي قسم أو الضغط خارج القائمة أو زر Escape.

## تحديث واجهة القراءة والقائمة

- تم تطبيق القائمة الجانبية على الموبايل والتابلت.
- تم منع زر القائمة من نقل الصفحة إلى الأعلى عند فتحه.
- تم تعديل أدوات القراءة: نقل أدوات الخط وملء الشاشة إلى شريط أدوات مستقل وأسفل نهاية السورة أيضًا.
- تم تغيير اسم وضع البطاقات إلى: قراءة بالآية.
- تم توسيع مساحة اسم السورة داخل شريط القارئ.
- تم ضبط نص القرآن ليكون مضبوطًا Justified في القراءة المتصلة وبطاقات الآيات.

## تحديثات هذه النسخة

- تثبيت شريط التطبيق في كل الصفحات ما عدا الرئيسية.
- إصلاح القائمة الجانبية على الموبايل والتابلت بحيث تنتقل للأقسام بشكل صحيح.
- تقليل طول منطقة الأدوات قبل عرض القرآن على الموبايل والتابلت.
- نقل أدوات القراءة داخل شريط القراءة/الشريط المتصل بدل شريط مستقل.
- إزالة زر حفظ الموضع لأن الحفظ تلقائي أثناء القراءة.
- تحويل حفظ الآيات والسورة إلى أيقونة قلب تتحول للأحمر عند الحفظ.
- إبقاء تغيير القارئ متاحًا.
- تحسين عرض اسم السورة وإظهار السابق/التالي حول منطقة العنوان.
- تعزيز ضبط نص القرآن Justify في القراءة المتصلة وقراءة الآية.

## تحديث أخير للرئيسية والهيدر

- تم إزالة `hero-ornament` من الصفحة الرئيسية لتقليل الفراغ وتحسين تجربة المستخدم.
- تم تثبيت `app-header` في كل الصفحات ما عدا الرئيسية، مع قواعد CSS نهائية تمنع تحركه أثناء التمرير.

## تحديث نهائي - تثبيت الهيدر وشريط القارئ

- تم إجبار `app-header` على الثبات في كل الصفحات ما عدا الرئيسية بقواعد CSS أقوى.
- تم إزالة أزرار `A-` و `A+` وملء الشاشة من `reader-toolbar glass-card`.
- بقيت أدوات الخط وملء الشاشة داخل `continuous-toolbar` في القراءة المتصلة.
- في عرض 1024px أو أقل أصبحت أزرار السابق، القارئ، التالي، ووضع القراءة في نفس السطر.
- تم ضبط زر السابق والتالي بنفس المقاس تقريبًا، مع تقليل عرض اختيار القارئ قليلًا ليظهر السطر بشكل متوازن.

## تحديث حفظ السورة عند التحديث

- عند فتح سورة ثم عمل Refresh للصفحة، يعود التطبيق لنفس السورة تلقائيًا.
- يتم حفظ آخر سورة وآخر آية محليًا في LocalStorage أثناء القراءة.
- زر ابدأ القراءة في الرئيسية يعتمد على آخر موضع محفوظ أو آخر سورة مفتوحة.

## تحديث أخير

- إصلاح فتح نفس السورة بعد عمل Refresh من داخل صفحة القارئ.
- تثبيت `app-header` في كل الصفحات ما عدا الرئيسية، مع تركه يتحرك طبيعيًا في الرئيسية.
- ضبط أدوات القارئ على الموبايل والتابلت بحيث تكون أزرار السابق/القارئ/التالي/وضع القراءة في صف واحد.
- تحسين معالجة أخطاء تحميل السورة حتى لا تبقى الصفحة على حالة "جاري التحميل" بدون رسالة واضحة.
